require 'spec_helper'

describe TeamMembershipsController do
  # fixtures :team_memberships
  # fixtures :users
  # fixtures :teams

  # before(:each) do
  #   @user = User.all[0]
  #   @team = Team.all[0]
  # end

  # describe "POST #join" do
  #   it "successfully responds with a HTTP 200 status code" do
  #     session[:user_id] = @user.id
  #     post :join, :team_name => @team.name
  #   end
  # end
end
